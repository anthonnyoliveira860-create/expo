import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.logo}>D.A</Text>
      <Text style={styles.subtitle}>
        Rede Social Minimalista
      </Text>

      <View style={styles.card}>
        <Text style={styles.text}>👁 Quem visitou meu perfil</Text>
        <Text style={styles.text}>❤️ Curtidas e comentários</Text>
        <Text style={styles.text}>🔒 Perfil privado</Text>
        <Text style={styles.text}>👥 Seguidores</Text>
      </View>

      <StatusBar style="light" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0A0A0A',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },

  logo: {
    color: '#00D9FF',
    fontSize: 48,
    fontWeight: 'bold',
    marginBottom: 10,
  },

  subtitle: {
    color: '#FFFFFF',
    fontSize: 18,
    marginBottom: 40,
  },

  card: {
    width: '100%',
    backgroundColor: '#1B1B1B',
    padding: 20,
    borderRadius: 20,
  },

  text: {
    color: '#FFFFFF',
    fontSize: 16,
    marginBottom: 15,
  },
});
